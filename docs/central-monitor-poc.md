# Central public monitor POC

This branch contains the proof-of-concept design for centrally analyzing failed workflow runs from public customer repositories without requiring the customer to store the NVIDIA API key.
